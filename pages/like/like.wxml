<!--like.wxml-->
<view class="container">
  <block wx:for="{{loveList}}" wx:for-index="index">  
    <view>   
      <image src="{{item.url}}"/> 
      <text>{{item.tex}}</text> 
    </view>   
  </block>  
</view>
